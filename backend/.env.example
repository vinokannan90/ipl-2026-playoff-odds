# Local-only example; copy to .env (gitignored) and fill in.
# In production, NEVER set IPLODDS_GITHUB_MODELS_TOKEN here — it comes from Key Vault.

IPLODDS_ENV=dev
IPLODDS_LOG_LEVEL=INFO
IPLODDS_CORS_ORIGINS=http://localhost:5173,http://localhost:8000

# LLM provider — choose one:
#   "azure_openai"  → uses Managed Identity in prod (no token, no rotation, recommended)
#   "github"        → uses GitHub Models PAT below (expires annually, free tier)
IPLODDS_LLM_PROVIDER=github
IPLODDS_GITHUB_MODELS_TOKEN=ghp_replace_me
IPLODDS_GITHUB_MODELS_MODEL=gpt-4o-mini

# Azure OpenAI (used when IPLODDS_LLM_PROVIDER=azure_openai)
# In production, endpoint is injected automatically by Bicep via Managed Identity — no key needed.
# For local dev with Azure OpenAI, set these two and change LLM_PROVIDER above.
IPLODDS_AZURE_OPENAI_ENDPOINT=https://<your-resource>.openai.azure.com/
IPLODDS_AZURE_OPENAI_DEPLOYMENT=gpt-4o-mini
# IPLODDS_AZURE_OPENAI_API_VERSION=2024-10-21  # only set to override the default

# Cache: leave blank to use in-memory cache locally
IPLODDS_BLOB_ACCOUNT_URL=

# Feature flags
IPLODDS_FEATURE_PRIORS=true
IPLODDS_FEATURE_AGENT=true
IPLODDS_FEATURE_LEVERAGE=true
IPLODDS_FEATURE_SCOUT=false

# CricAPI (cricketdata.org) — full scorecard via get_scorecard agent tool
# Get a free key at https://cricketdata.org/signup.aspx (100 req/day)
IPLODDS_CRICKETDATA_API_KEY=xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx
# Optional: set once IPL 2026 is listed in CricAPI; auto-discovered otherwise
IPLODDS_CRICKETDATA_IPL_SERIES_ID=
