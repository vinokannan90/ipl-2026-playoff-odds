{
  "$schema": "https://schema.management.azure.com/schemas/2019-04-01/deploymentParameters.json#",
  "contentVersion": "1.0.0.0",
  "parameters": {
    "namePrefix": { "value": "${AZURE_NAME_PREFIX=iplodds}" },
    "location":   { "value": "${AZURE_LOCATION=centralindia}" },
    "backendImage": { "value": "${SERVICE_API_IMAGE_NAME=mcr.microsoft.com/k8se/quickstart:latest}" },
    "githubModelsToken": { "value": "${GITHUB_MODELS_TOKEN}" }
  }
}
